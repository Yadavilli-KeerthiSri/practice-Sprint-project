<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Payment Form</title>
    <link href="https://cdn.jsdelivr.net" rel="stylesheet">
    <style>
        body { background-color: #f4f7f6; padding-top: 50px; }
        .form-card { background: white; padding: 30px; border-radius: 12px; box-shadow: 0 10px 25px rgba(0,0,0,0.05); max-width: 500px; margin: auto; }
    </style>
</head>
<body>
<div class="container">
    <div class="form-card">
        <h2 class="mb-4 text-center" th:text="${payment.paymentId == null ? 'New Payment' : 'Update Payment'}">Payment</h2>
        <hr>
        <form th:action="@{/payments/save}" th:object="${payment}" method="post">
            <input type="hidden" th:field="*{paymentId}" />

            <!-- Payment Method -->
            <div class="mb-3">
                <label class="form-label fw-bold">Payment Method</label>
                <input type="text" th:field="*{paymentName}" class="form-control" 
                       th:errorclass="is-invalid" placeholder="e.g. UPI, Credit Card, COD" />
                <div class="invalid-feedback" th:if="${#fields.hasErrors('paymentName')}" th:errors="*{paymentName}"></div>
            </div>

            <!-- Amount -->
            <div class="mb-3">
                <label class="form-label fw-bold">Amount (₹)</label>
                <input type="number" step="0.01" th:field="*{amount}" class="form-control" 
                       th:errorclass="is-invalid" placeholder="0.00" />
                <div class="invalid-feedback" th:if="${#fields.hasErrors('amount')}" th:errors="*{amount}"></div>
            </div>

            <!-- Transaction Status Enum -->
            <div class="mb-3">
                <label class="form-label fw-bold">Status</label>
                <select th:field="*{transactionStatus}" class="form-select" th:errorclass="is-invalid">
                    <option value="" disabled selected>-- Select Status --</option>SUCCESS</option>
                </select>
                <div class="invalid-feedback" th:if="${#fields.hasErrors('transactionStatus')}" th:errors="*{transactionStatus}"></div>
            </div>

            <div class="d-grid gap-2 mt-4">
                <button type="submit" class="btn btn-primary">Save Transaction</button>
                <a th:href="@{/payments}" class="btn btn-light">Cancel</a>
            </div>
        </form>
    </div>
</div>
</body>
</html>
