<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orders Management | Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-slate-50 min-h-screen p-8">

    <div class="max-w-7xl mx-auto">
        <!-- Header Section -->
        <div class="flex justify-between items-end mb-8">
            <div>
                <h2 class="text-3xl font-bold text-slate-900 tracking-tight">Live Orders</h2>
                <p class="text-slate-500 text-sm">Monitor transactions and update delivery progress</p>
            </div>
            <div class="text-right">
                <span class="text-xs font-bold uppercase text-slate-400 block mb-1">System Status</span>
                <span class="bg-indigo-100 text-indigo-700 px-3 py-1 rounded-full text-xs font-bold">Real-time Sync Active</span>
            </div>
        </div>

        <!-- Orders Table Card -->
        <div class="bg-white rounded-2xl shadow-sm border border-slate-200 overflow-hidden">
            <div class="overflow-x-auto">
                <table class="w-full text-left border-collapse">
                    <thead>
                        <tr class="bg-slate-50/50 border-b border-slate-200">
                            <th class="px-6 py-4 text-xs font-bold uppercase tracking-wider text-slate-500">Order ID</th>
                            <th class="px-6 py-4 text-xs font-bold uppercase tracking-wider text-slate-500">Customer</th>
                            <th class="px-6 py-4 text-xs font-bold uppercase tracking-wider text-slate-500">Current Status</th>
                            <th class="px-6 py-4 text-xs font-bold uppercase tracking-wider text-slate-500">Total</th>
                            <th class="px-6 py-4 text-xs font-bold uppercase tracking-wider text-slate-500">Payment Info</th>
                            <th class="px-6 py-4 text-xs font-bold uppercase tracking-wider text-slate-500 text-center">Update Progress</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-100">
                        <tr th:each="o : ${orders}" class="hover:bg-indigo-50/30 transition-colors group">
                            <!-- ID -->
                            <td class="px-6 py-4">
                                <span class="font-mono text-xs font-bold text-slate-400" th:text="${'#ORD-' + o.orderId}"></span>
                            </td>
                            
                            <!-- Customer -->
                            <td class="px-6 py-4">
                                <div class="flex items-center gap-3">
                                    <div class="w-8 h-8 rounded-full bg-slate-100 flex items-center justify-center text-xs font-bold text-slate-500">
                                        <span th:text="${#strings.substring(o.customer.customerName,0,1)}"></span>
                                    </div>
                                    <span class="font-semibold text-slate-800" th:text="${o.customer.customerName}"></span>
                                </div>
                            </td>

                            <!-- Status Badge -->
                            <td class="px-6 py-4">
                                <span class="inline-flex items-center px-2.5 py-1 rounded-md text-xs font-bold border"
                                      th:classappend="${o.orderStatus == 'Delivered' ? 'bg-green-50 text-green-700 border-green-200' : 
                                                       (o.orderStatus == 'Cancelled' ? 'bg-red-50 text-red-700 border-red-200' : 'bg-indigo-50 text-indigo-700 border-indigo-200')}"
                                      th:text="${o.orderStatus}">
                                </span>
                            </td>

                            <!-- Total -->
                            <td class="px-6 py-4">
                                <span class="text-slate-900 font-bold" th:text="${'₹' + o.totalAmount}"></span>
                            </td>

                            <!-- Payment & Transaction -->
                            <td class="px-6 py-4">
                                <div class="flex flex-col">
                                    <span class="text-sm font-medium text-slate-700" th:text="${o.payment.paymentMethod}"></span>
                                    <span class="text-[10px] font-bold uppercase tracking-tighter" 
                                          th:classappend="${o.payment.transactionStatus == 'Success' ? 'text-green-500' : 'text-amber-500'}"
                                          th:text="${o.payment.transactionStatus}"></span>
                                </div>
                            </td>

                            <!-- Action -->
                            <td class="px-6 py-4 text-center">
                                <form th:action="@{/admin/orders/update/{id}(id=${o.orderId})}" method="post">
                                    <button type="submit" class="inline-flex items-center gap-2 bg-white border border-indigo-200 text-indigo-600 hover:bg-indigo-600 hover:text-white px-4 py-1.5 rounded-lg text-sm font-bold shadow-sm transition-all active:scale-95">
                                        Move to Next <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path></svg>
                                    </button>
                                </form>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Footer Navigation -->
        <div class="mt-8 flex justify-between items-center">
            <a th:href="@{/admin/dashboard}" class="text-slate-400 hover:text-indigo-600 text-sm font-medium transition-colors">
                ← Back to Overview
            </a>
            <p class="text-slate-400 text-xs">Viewing latest orders for all restaurants</p>
        </div>
    </div>

</body>
</html>
