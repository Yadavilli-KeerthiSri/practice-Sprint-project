<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | FoodExec</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            /* Solid black background as requested */
            background-color: #000000; 
            overflow: hidden; /* Prevents scrollbars from floating icons */
        }

        .glass-card {
            /* Adjusted card style for high contrast on black background */
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            position: relative;
            z-index: 10;
        }

        /* --- Floating Icons CSS --- */
        .food-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
            pointer-events: none;
        }

        .floating-item {
            position: absolute;
            /* Start slightly off the left side */
            left: -100px; 
            font-size: 2.5rem;
            opacity: 0.2; /* Subtle visibility */
            animation: floatRight linear infinite;
            /* Added vertical position variable for variety */
            top: calc(var(--v) * 1vh); 
        }

        /* Keyframe animation updated to move LEFT to RIGHT */
        @keyframes floatRight {
            0% {
                transform: translateX(0) rotate(0deg);
                opacity: 0;
            }
            10% { opacity: 0.2; }
            90% { opacity: 0.2; }
            100% {
                /* End off the right side */
                transform: translateX(110vw) rotate(360deg);
                opacity: 0;
            }
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center p-6">

    <!-- Floating Food Items (Emojis) -->
    <div class="food-container">
        <!-- Different vertical positions (--v) and durations for variety -->
        <span class="floating-item" style="--v:10; animation-duration: 20s;">🍕</span>
        <span class="floating-item" style="--v:25; animation-duration: 25s; animation-delay: 2s;">🍔</span>
        <span class="floating-item" style="--v:40; animation-duration: 22s; animation-delay: 5s;">🍟</span>
        <span class="floating-item" style="--v:55; animation-duration: 28s;">🌮</span>
        <span class="floating-item" style="--v:70; animation-duration: 24s; animation-delay: 3s;">🍣</span>
        <span class="floating-item" style="--v:85; animation-duration: 18s; animation-delay: 7s;">🍜</span>
    </div>

    <div class="max-w-md w-full glass-card rounded-[2.5rem] shadow-2xl shadow-slate-900/40 p-10">
        
        <!-- Logo & Header (Text is still dark, so it contrasts with the white card) -->
        <div class="text-center mb-8">
            <div class="inline-flex items-center justify-center w-16 h-16 bg-orange-100 text-orange-500 rounded-2xl mb-4 text-3xl">
                🍕
            </div>
            <h2 class="text-3xl font-black text-slate-800 tracking-tight">Welcome Back</h2>
            <p class="text-slate-500 mt-2 font-medium">Log in to order your favorites!</p>
        </div>

        <!-- Alert Messages -->
        <div th:if="${param.error}" class="mb-6 p-4 bg-rose-50 border border-rose-100 text-rose-600 text-sm font-semibold rounded-2xl text-center animate-bounce">
            ❌ Invalid Email or Password
        </div>

        <div th:if="${param.logout}" class="mb-6 p-4 bg-emerald-50 border border-emerald-100 text-emerald-600 text-sm font-semibold rounded-2xl text-center">
            👋 Logged out successfully
        </div>

        <!-- Login Form -->
        <form th:action="@{/doLogin}" method="post" class="space-y-5">
            <div>
                <label class="block text-xs font-bold text-slate-400 uppercase tracking-widest mb-2 ml-1">Email Address</label>
                <input type="email" name="email" placeholder="name@company.com" required
                    class="w-full px-5 py-4 bg-white border border-slate-100 rounded-2xl focus:ring-2 focus:ring-orange-400 focus:border-orange-400 outline-none transition-all placeholder:text-slate-300 shadow-sm">
            </div>

            <div>
                <label class="block text-xs font-bold text-slate-400 uppercase tracking-widest mb-2 ml-1">Password</label>
                <input type="password" name="password" placeholder="••••••••" required
                    class="w-full px-5 py-4 bg-white border border-slate-100 rounded-2xl focus:ring-2 focus:ring-orange-400 focus:border-orange-400 outline-none transition-all placeholder:text-slate-300 shadow-sm">
            </div>

            <div class="pt-2">
                <button type="submit" class="w-full bg-orange-500 hover:bg-orange-600 text-white font-bold py-4 rounded-2xl shadow-lg shadow-orange-200 transition-all transform active:scale-95">
                    Login to Account
                </button>
            </div>
        </form>

        <div class="mt-8 text-center border-t border-slate-100 pt-6">
            <p class="text-slate-400 text-sm font-medium">
                New to the platform? 
                <a th:href="@{/register}" class="text-orange-500 hover:text-orange-600 font-bold ml-1 transition-colors">
                    Create an account
                </a>
            </p>
        </div>
    </div>

</body>
</html>
