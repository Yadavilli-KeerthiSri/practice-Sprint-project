<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Customer Management</title>
    <link href="https://cdn.jsdelivr.net" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; padding-top: 50px; }
        .table-container { background: white; padding: 30px; border-radius: 10px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
    </style>
</head>
<body>
<div class="container">
    <div class="table-container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="text-primary">Customers</h2>
            <a th:href="@{/customers/new}" class="btn btn-primary">Add New Customer</a>
        </div>
        
        <table class="table table-hover">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Contacts</th>
                    <th>Address</th>
                    <th class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                <tr th:each="cust : ${customers}">
                    <td th:text="${cust.customerId}">1</td>
                    <td th:text="${cust.customerName}" class="fw-bold"></td>
                    <td th:text="${cust.email}"></td>
                    <td th:text="${cust.contacts}"></td>
                    <td th:text="${cust.address}"></td>
                    <td class="text-center">
                        <a th:href="@{/customers/edit/{id}(id=${cust.customerId})}" class="btn btn-sm btn-warning">Edit</a>
                        <a th:href="@{/customers/delete/{id}(id=${cust.customerId})}" 
                           class="btn btn-sm btn-danger" 
                           onclick="return confirm('Are you sure?')";>Delete</a>
                    </td>
                </tr>
                <tr th:if="${#lists.isEmpty(customers)}">
                    <td colspan="6" class="text-center text-muted">No customers found.</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
</body>
</html>
