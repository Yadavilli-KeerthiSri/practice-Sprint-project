<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" xmlns:sec="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net" rel="stylesheet">
    <style>
        body { background: #f4f6f8; font-family: Arial, sans-serif; }
        .container { padding: 30px; }
        .card { background: white; padding: 20px; border-radius: 12px; box-shadow: 0 6px 15px rgba(0,0,0,0.1); text-align: center; margin-bottom: 20px;}
        a { text-decoration: none; color: #ff5722; font-weight: bold; }
    </style>
</head>
<body>
    <!-- Use ~{} syntax to ensure the fragment is found correctly -->
    <div th:replace="~{fragments/navbar :: body}"></div>

    <div class="container">
        <h2 class="mb-4">Welcome to Dashboard</h2>
        <div class="row">
            <!-- ADMIN CARDS -->
            <div class="col-md-4" sec:authorize="hasRole('ADMIN')">
                <div class="card">
                    <h3>Manage Orders</h3>
                    <a th:href="@{/admin/orders/list}">Open Table</a>
                </div>
            </div>
            <!-- USER CARDS -->
            <div class="col-md-4" sec:authorize="hasRole('USER')">
                <div class="card">
                    <h3>My Orders</h3>
                    <a th:href="@{/orders/history}">View Status</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
