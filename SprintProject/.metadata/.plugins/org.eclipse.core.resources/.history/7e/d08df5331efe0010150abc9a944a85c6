package com.cg.controller;

import com.cg.entity.Customer;
import com.cg.exception.ResourceNotFound;
import com.cg.service.CustomerService;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/customer")
public class CustomerController {
	@Autowired
    private CustomerService customerService;
 
    // VIEW ALL CUSTOMERS
    @GetMapping
    public String getAllCustomers(Model model) {
        model.addAttribute("customers", customerService.getAllCustomers());
        return "customers";
    }
 
    // ADD CUSTOMER
    @PostMapping("/add")
    public String addCustomer(Customer customer) {
        customerService.createCustomer(customer);
        return "redirect:/customers";
    }
 
    // EDIT CUSTOMER
    @PostMapping("/update/{id}")
    public String updateCustomer(@PathVariable Integer id, Customer customer) throws ResourceNotFound {
        customerService.updateCustomer(id, customer);
        return "redirect:/customers";
    }
 
    // DELETE CUSTOMER
    @GetMapping("/delete/{id}")
    public String deleteCustomer(@PathVariable Integer id) throws ResourceNotFound {
        customerService.deleteCustomer(id);
        return "redirect:/customers";
    }
}