<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Search Results | FoodExec</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com" rel="stylesheet" />
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-slate-50 min-h-screen p-6 md:p-12">

    <div class="max-w-6xl mx-auto">
        <!-- Header -->
        <header class="mb-10 flex flex-col md:flex-row justify-between items-center gap-4">
            <div class="flex items-center gap-4">
                <div class="bg-sky-500 text-white p-3 rounded-2xl shadow-lg shadow-sky-100">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                </div>
                <h1 class="text-3xl font-extrabold text-slate-800 tracking-tight">
                    Results for "<span th:text="${keyword}"></span>"
                </h1>
            </div>
            <a th:href="@{/user/dashboard}" class="text-sm font-bold text-slate-400 hover:text-sky-600 transition-colors">← Back to Dashboard</a>
        </header>

        <!-- Section: Restaurants -->
        <div class="mb-16">
            <div class="flex items-center gap-3 mb-6">
                <h2 class="text-xl font-bold text-slate-700">Matched Restaurants</h2>
                <span class="px-2 py-0.5 bg-sky-100 text-sky-600 rounded-md text-xs font-bold" 
                      th:text="${restaurants != null ? #lists.size(restaurants) : 0}">0</span>
            </div>

            <div th:if="${restaurants == null || #lists.isEmpty(restaurants)}" class="bg-white rounded-3xl p-10 text-center border border-dashed border-slate-200">
                <p class="text-slate-400 font-medium italic">No restaurants found matching your criteria.</p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6" th:if="${restaurants != null}">
                <div th:each="r : ${restaurants}" class="bg-white p-6 rounded-[2rem] shadow-sm border border-slate-100 hover:shadow-xl hover:shadow-sky-100/50 transition-all group">
                    <div class="flex justify-between items-start mb-4">
                        <div class="w-12 h-12 bg-sky-50 rounded-xl flex items-center justify-center text-xl">🏪</div>
                        <span class="bg-amber-50 text-amber-600 px-3 py-1 rounded-full text-xs font-bold border border-amber-100">
                             <span th:text="${r.ratings != null ? r.ratings : '0.0'}">0</span> ★
                        </span>
                    </div>
                    <h3 class="text-lg font-bold text-slate-800 mb-1" th:text="${r.restaurantName ?: 'Unknown Restaurant'}">Name</h3>
                    <p class="text-slate-400 text-sm mb-4 flex items-center gap-1">
                        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path></svg>
                        <span th:text="${r.address ?: 'No Address'}">Location</span>
                    </p>
                    <span class="inline-block px-3 py-1 bg-slate-50 text-slate-500 rounded-lg text-[10px] font-bold uppercase tracking-wider" 
                          th:text="${r.cuisine ?: 'General'}">Cuisine</span>
                </div>
            </div>
        </div>

        <!-- Section: Menu Items -->
        <div>
            <div class="flex items-center gap-3 mb-6">
                <h2 class="text-xl font-bold text-slate-700">Matched Dishes</h2>
                <span class="px-2 py-0.5 bg-emerald-100 text-emerald-600 rounded-md text-xs font-bold" 
                      th:text="${menuItems != null ? #lists.size(menuItems) : 0}">0</span>
            </div>

            <div th:if="${menuItems == null || #lists.isEmpty(menuItems)}" class="bg-white rounded-3xl p-10 text-center border border-dashed border-slate-200">
                <p class="text-slate-400 font-medium italic">No menu items found matching your criteria.</p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6" th:if="${menuItems != null}">
                <div th:each="item : ${menuItems}" class="bg-white p-6 rounded-[2rem] shadow-sm border border-slate-100 flex items-center justify-between group hover:border-emerald-200 transition-all">
                    <div class="flex items-center gap-4">
                        <div class="w-16 h-16 bg-emerald-50 rounded-2xl flex items-center justify-center text-2xl group-hover:scale-110 transition-transform">🍔</div>
                        <div>
                            <span class="text-[10px] font-bold text-emerald-500 uppercase tracking-widest" th:text="${item.category ?: 'Food'}">Category</span>
                            <h3 class="text-lg font-bold text-slate-800" th:text="${item.itemName ?: 'Unnamed Item'}">Item Name</h3>
                            <p class="text-emerald-600 font-black text-xl">₹ <span th:text="${item.price ?: 0}">0</span></p>
                        </div>
                    </div>
                    
                    <form th:action="@{/cart/add}" method="post">
                        <!-- Check: If your Java field is 'id', change this to item.id -->
                        <input type="hidden" name="itemId" th:value="${item.itemId}">
                        <button type="submit" class="bg-emerald-500 hover:bg-emerald-600 text-white font-bold px-6 py-3 rounded-xl shadow-lg shadow-emerald-100 transition-all active:scale-95 flex items-center gap-2">
                            <span>Add</span>
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path></svg>
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

</body>
</html>
